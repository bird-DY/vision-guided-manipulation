# 上游归档配置

本目录中的 MoveIt 配置仅保留用于对照原始 LeoRobot 导入内容，不再作为运行时配置源。

权威 MoveIt、ros2_control 和控制器配置位于同级功能包 `mybot/config`。所有项目启动文件必须从 `mybot` 加载配置；不得修改此目录后声称运行配置已经生效。

保留这些文件是为了方便审计上游差异。完成独立的历史归档后可以整体删除，但不能让两套配置重新同时参与启动。
